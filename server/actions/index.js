'use strict';
import {
  exchangeToken,
  userProfile,
} from './facebook-webkit';

import {
  findAccountKitUser,
  registerAccountKitUser,
} from './user';

module.exports = {
  exchangeToken,
  userProfile,
  
  findAccountKitUser,
  registerAccountKitUser,
};